<!DOCTYPE html>
<html>
  <head>
    <style>
      body { font-family: Arial, sans-serif; background: #f9f9f9; padding: 20px; }
      .container { background: #fff; padding: 20px; border-radius: 10px; max-width: 600px; margin: auto; box-shadow: 0 2px 6px rgba(0,0,0,0.1);}
      h2 { color: #2c3e50; }
      .status { font-weight: bold; color: #e74c3c; }
      .btn {
        display: inline-block;
        padding: 10px 20px;
        margin-top: 20px;
        background: #3498db;
        color: #fff;
        border-radius: 6px;
        text-decoration: none;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h2>Hello {{guideName}},</h2>
      <p>
        The destination <b>{{destinationName}}</b> has been
        <span class="status">{{statusText}}</span> by the admin.
      </p>
      <p>{{adminNote}}</p>
      <a href="{{link}}" class="btn">View Destination</a>
    </div>
  </body>
</html>
